image: quay-its.epfl.ch/svc0176/umami
version: 3.0.3
app: umami
subdomain: umami
secrets: "{{ lookup('pipe', 'keybase fs read /keybase/team/epfl_sopec/umami/secrets.yml') | from_yaml }}"
domain: fsd.epfl.ch

postgres_cluster:
  name: postgres-cluster
