image: quay-its.epfl.ch/svc0176/rybbit
version: latest
app: rybbit
subdomain: rybbit
secrets: "{{ lookup('pipe', 'keybase fs read /keybase/team/epfl_sopec/rybbit/secrets.yml') | from_yaml }}"
domain: labs.fsd.team

postgres_cluster:
  name: postgres-cluster