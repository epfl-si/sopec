image: ghcr.io/goauthentik/server
version: 2025.8.3
app: authentik
subdomain: auth
secrets: "{{ lookup('pipe', 'keybase fs read /keybase/team/epfl_sopec/authentik/secrets.yml') | from_yaml }}"
domain: fsd.epfl.ch

postgres_cluster:
  name: postgres-cluster
