apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: svc0176t-isas-fsd

resources:
  - ../../base

patches:
  - target:
      kind: Route
      name: umami
    patch: |-
      - op: replace
        path: /spec/host
        value: umami-test.fsd.epfl.ch
