apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: svc0176t-isas-fsd

resources:
  - ../../base

patches:
  - target:
      kind: Route
      name: 1time-web
    patch: |-
      - op: replace
        path: /spec/host
        value: 1time-test.fsd.epfl.ch
  - target:
      kind: Deployment
      name: 1time-web
    patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: 1time-web
      spec:
        template:
          spec:
            containers:
              - name: 1time-web
                env:
                  - name: APP_HOSTNAME
                    value: 1time-test.fsd.epfl.ch
