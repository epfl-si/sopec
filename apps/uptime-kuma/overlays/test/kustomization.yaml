apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: svc0176t-isas-fsd

resources:
  - ../../base

patches:
  - target:
      kind: Route
      name: uptime-kuma
    patch: |-
      - op: replace
        path: /spec/host
        value: kuma-test.fsd.epfl.ch
