apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: svc0176t-isas-fsd

resources:
  - ../../base

patches:
  - target:
      kind: Route
      name: boardfsdash
    patch: |-
      - op: replace
        path: /spec/host
        value: dash-test.fsd.epfl.ch
